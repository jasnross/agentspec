---
description: A basic skill with no supporting files
---

# Basic Skill

> This instruction is shared via fragment inclusion.

If clarification is needed, use the `question` tool to ask. If parallel work is needed, dispatch with the `task` tool. If a named helper is needed, invoke it with the `skill` tool.

Skill instructions here.

Agent: test-agent
